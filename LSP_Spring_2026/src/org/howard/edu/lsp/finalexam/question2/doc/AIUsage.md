AI Tools Used: Claude (claude.ai)

Prompts Used:
1. "How does the Template Method design pattern work in Java?"
3. "Show a driver class that uses a List<Report> and calls generateReport() polymorphically."

How AI Helped:
AI clarified the role of the final template method versus the abstract hook methods that
subclasses override. It helped structure the class hierarchy and driver to demonstrate
polymorphism correctly.

Reflection:
I learned that marking the template method as final is important to prevent subclasses from
breaking the intended workflow. The pattern separates the algorithm structure from its
specific implementations.