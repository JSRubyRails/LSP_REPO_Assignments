AI Tools Used: Claude

Prompts Used:
1. "What are good boundary value test cases for a grade calculator with scores 0–100?"
2. "How do I use assertThrows() in JUnit 5 to test for IllegalArgumentException?"

How AI Helped:
AI helped identify meaningful boundary values (exactly 60 and exactly 90) that test the
edges of the grading logic. It also demonstrated the correct JUnit 5 assertThrows() syntax
with lambda expressions.

Reflection:
I learned that boundary value testing is most useful at the exact cutoff points in
conditional logic, not just typical mid-range values.